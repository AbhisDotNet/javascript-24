"use strict"; // it treats all the JS code a newer version  

//alert(3+3) // this can only be run in the broswer not in node.

let name ="ram"
let age =15
let isLogedIn=false

console.log(typeof "age");
console.log(typeof null); // in an object
console.log(typeof undefined);